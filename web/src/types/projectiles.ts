// Tipos de proyectiles por dotación. La forma de la API viene GENERADA desde
// los structs Go (web/src/types/generated/projectiles.ts, `make types`).
// Aquí solo se re-exportan con los nombres del frontend.

export type {
    CrewProjectiles as CrewProjectilesData,
    Result as ProjectilesResponse,
} from './generated/projectiles';
