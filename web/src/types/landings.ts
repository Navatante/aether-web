// Tipos de tomas y aproximaciones por piloto. La forma de la API viene GENERADA
// desde los structs Go (web/src/types/generated/landings.ts, `make types`).
// Aquí solo se re-exportan con los nombres del frontend.

export type {
    PilotLandingsApproaches as PilotLandingsApproachesData,
    Result as LandingsApproachesResponse,
} from './generated/landings';
